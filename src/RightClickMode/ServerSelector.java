package RightClickMode;

import javax.swing.*;

public class ServerSelector extends JFrame {
    public ServerSelector() {
        JOptionPane.showMessageDialog(null,
                "Not implemented yet. Launch the app without any arguments to go back to normal mode.", "Hello", JOptionPane.INFORMATION_MESSAGE);
        System.exit(1);
    }
}
